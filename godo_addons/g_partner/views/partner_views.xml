<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="g_partner_main_list_view" model="ir.ui.view">
            <field name="name">res.partner.tree</field>
            <field name="model">res.partner</field>
            <field eval="8" name="priority" /> 
            <field name="arch" type="xml">
                <tree string="Contacts" sample="1" multi_edit="1"> 
                    <field name="display_name" string="Name" />
                    <field name="function" invisible="1" />
                    <field name="phone" class="o_force_ltr" optional="show" />
                    <field name="email" optional="show" />  
                    <field name="vat" optional="hide" readonly="1" /> 
                    <field name="province_id" optional="show"/>
                    <field name="district_id" optional="show"/>
                    <field name="street" />
                    <field name="category_id" optional="hide" widget="many2many_tags"
                        options="{'color_field': 'color'}" />
                    <field name="company_id" groups="base.group_multi_company" readonly="1" />
                    <field name="is_company" optional="hide"  />
                    <field name="parent_id" invisible="1" readonly="1" />
                    <field name="active" invisible="1" />
                </tree>
            </field>
        </record>

        <record id="g_partner_main_search_view" model="ir.ui.view">
            <field name="name">res.partner.search</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
                <search string="Customer">
                    <field name="name" string="Customer" filter_domain="['|', ('name', 'ilike', self), ('mobile', 'ilike', self), ('street', 'ilike', self)]"/>
                    
                    <group expand="1" string="Nhóm theo">
                        <filter string="Nhóm cha" name="filter_by_parent_id" context="{'group_by':'parent_id'}"/> 
                    </group>
                </search>
            </field>
        </record>

        <record model="ir.actions.act_window" id="g_partner_main_action_window">
            <field name="name">Partner</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">tree,form</field>
        </record>

        <record id="g_partner_main_action_window_tree" model="ir.actions.act_window.view">
            <field name="act_window_id" ref="g_partner_main_action_window" />
            <field name="sequence" eval="1" />
            <field name="view_mode">tree</field>
            <field name="view_id" ref="g_partner_main_list_view" />
        </record>

        <!-- <record id="g_partner_main_action_window_form" model="ir.actions.act_window.view">
            <field name="act_window_id" ref="g_partner_main_action_window" />
            <field name="sequence" eval="2" />
            <field name="view_mode">form</field>
            <field name="view_id" ref="g_partner_main_form_view" />
        </record> -->

    </data>
</odoo>
